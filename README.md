
  # Mobile Course Search UI

  This repo contains a small Vite + React app for “Mobile Course Search UI”.

  - **Design source**: `https://www.figma.com/design/tYZSRE6tPXALhqWrV31NSD/Mobile-Course-Search-UI`

  ## Running the code

  - **Install dependencies**:
    - `npm install`
  - **Start the dev server**:
    - `npm run dev`
    - Then open the URL shown in the terminal (typically `http://localhost:5173`).
  - **Build for production**:
    - `npm run build`
  - **Preview the production build locally** (after building):
    - `npx vite preview`

  ## Attribution

  The code in this repository was generated with **Figma Make** and then modified by the author for this assignment/project.
 
